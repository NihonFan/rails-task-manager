<h1>this is the list of tasks/show file</h1>

<p><%= "#{@task.completed} #{@task.title}" %></p>
<p><%=  "#{@task.details}(this should be a hover)" %></p>
<br>
<p><%= link_to 'Back to main display', tasks_path %></p>
<p><%= link_to 'modify this task', edit_task_path(@task.id) %></p>
<p><%= link_to 'delete', task_path(@task.id), method: :delete, data: { confirm: 'Are you sure?' } %></p>


